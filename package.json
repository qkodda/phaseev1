{
  "name": "phasee",
  "version": "1.0.0",
  "type": "module",
  "description": "Phasee - Experiential Storytelling Content Generator",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 4000",
    "build": "vite build",
    "preview": "vite preview",
    "cap:sync": "npm run build && npx cap sync",
    "cap:ios": "npm run cap:sync && npx cap open ios",
    "cap:build": "npm run build && npx cap sync ios"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@capacitor/cli": "^7.4.4",
    "@capacitor/core": "^7.4.4",
    "@capacitor/ios": "^7.4.4",
    "@supabase/supabase-js": "^2.80.0",
    "openai": "^6.8.1"
  }
}
