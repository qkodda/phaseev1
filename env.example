# ============================================
# PHAZEE ENVIRONMENT CONFIGURATION
# ============================================

# ------------------------------------------
# CLIENT-SIDE VARIABLES (VITE_ prefix)
# These are safe to expose to the browser
# ------------------------------------------

# Supabase Configuration
# Get these from your Supabase project: Settings > API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Production URL (for email verification links)
# Set this to your production URL in production environment
VITE_SITE_URL=https://your-app.vercel.app

# Dev Bypass (LOCAL DEVELOPMENT ONLY - DO NOT SET IN PRODUCTION)
# Set to 'true' to skip authentication during development
# VITE_DEV_BYPASS_AUTH=true

# ------------------------------------------
# SERVER-SIDE VARIABLES (NO VITE_ prefix)
# These are NEVER exposed to the browser
# Set these in Vercel Dashboard > Environment Variables
# ------------------------------------------

# OpenAI API Key (SERVER-SIDE ONLY)
# IMPORTANT: Do NOT add VITE_ prefix - this must stay server-side
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# Supabase Service Role Key (SERVER-SIDE ONLY)
# For admin operations - never expose this
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Admin Secret Key (for admin API endpoints)
ADMIN_SECRET_KEY=your-admin-secret-here

# ------------------------------------------
# INSTRUCTIONS
# ------------------------------------------
# 1. Copy this file to .env.local for local development
# 2. For production, set variables in Vercel Dashboard
# 3. NEVER commit .env.local to version control
# 4. NEVER add VITE_ prefix to sensitive keys (OpenAI, Service Role)

